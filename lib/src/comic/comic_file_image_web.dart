import 'package:flutter/painting.dart';

ImageProvider fileImageProvider(String path) {
  throw UnsupportedError(
    'ComicPages.fromFiles is not supported on web. Use fromUrls or fromBytes.',
  );
}
