# 测试签名（可提交仓库）

仅用于 `phantasm_read` **示例应用**本地 / CI 打包，**不可**用于正式发布。

| 项 | 值 |
|----|-----|
| 文件 | `phantasm_read_test.jks` |
| alias | `phantasm_test` |
| storePassword / keyPassword | `phantasm123` |
| 有效期 | 约 10000 天 |

Gradle 配置：`example/android/key.properties`
